# Server Configuration
PORT=3000

# Tunneling Configuration
# Set ENABLE_TUNNEL=true to automatically start tunnel when server starts
ENABLE_TUNNEL=false

# Tunnel service: 'localtunnel' or 'ngrok'
TUNNEL_SERVICE=localtunnel

# Optional: Custom subdomain for localtunnel (must be available)
# TUNNEL_SUBDOMAIN=football-match

# Optional: Ngrok auth token (for custom domains and more features)
# NGROK_AUTH_TOKEN=your_ngrok_auth_token_here
